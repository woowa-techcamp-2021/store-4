import React from 'react';

import styled from 'styled-components';

const SortButton = (): React.ReactElement => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default SortButton;
